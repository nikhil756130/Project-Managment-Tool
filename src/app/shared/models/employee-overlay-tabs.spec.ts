import { EmployeeOverlayTabs } from './employee-overlay-tabs';

describe('EmployeeOverlayTabs', () => {
  it('should create an instance', () => {
    expect(new EmployeeOverlayTabs()).toBeTruthy();
  });
});
